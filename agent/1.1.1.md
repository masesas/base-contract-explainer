referensi:
- terdapat deskripsi seperti di bawah ini di basescan api dashboard:
	```
	Access BaseScan data with Etherscan API V2
	API access for data on BASE is provided through Etherscan API V2. A single API key can be used to query data for BASE and more than 60 other EVM chains.
	```
- etherscan 
	```
	https://docs.etherscan.io/api-reference/endpoint/tokensupply
	```
- saya telah memasukkan API key dari etherscan di .env.local

instruksi:
- load memory dari bead cli dan eksekusi sesuai petunjuk dari memory
- jika tidak memungkinkan untuk menggunakan API dari basescan maka buatlah summarize
- simpan ke memory dengan bead cli hasil dari summarize dokumentasi etherscan
- buatkan saya function-function yg relevan agar goal project ini bisa terealisasikan